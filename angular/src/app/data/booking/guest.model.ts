/**
 * Represents the _Guest_ model
 *
 * ```yaml
 * guestId: number;
 * guestType: string;
 * guestFirstName: string;
 * guestLastName: string;
 * ```
 */

export class Guest {
  guestId: number;
  guestType: string;
  guestFirstName: string;
  guestLastName: string;
}
